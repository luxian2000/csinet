# 环境配置说明

## 默认环境：penny

此仓库的默认Python环境已设置为 **penny**。

### 激活环境

```bash
conda activate penny
```

### 环境信息

- **Python 版本**: 3.11.8
- **环境文件**: `environment.yml`
- **配置文件**: `.env`

### 还原环境

如需重新创建penny环境，可使用以下命令：

```bash
conda env create -f environment.yml
```

### IDE 配置

如使用 VS Code，应确保选择 penny 环境作为 Python 解释器。
